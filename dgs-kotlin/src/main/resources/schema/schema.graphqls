type Query {
    countries(countryFilter: String): [Country]
}

type Mutation {
    addCountry(name: String!, positiveCases: Int!): [Country]
}

type Subscription {
    latest : [Country]
}

type Country {
    name: String
    positiveCases: Int
}